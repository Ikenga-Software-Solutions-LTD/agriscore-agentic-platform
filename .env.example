# Optional OpenAI-compatible LLM configuration.
# Leave unset to use the deterministic explanation fallback.
AGRI_LLM_API_KEY=
AGRI_LLM_BASE_URL=
AGRI_LLM_MODEL=gpt-5
AGRI_LLM_TIMEOUT_SECONDS=60

# Example only — never commit real values.
# For a compatible endpoint, AGRI_LLM_BASE_URL should omit the final /chat/completions path.
